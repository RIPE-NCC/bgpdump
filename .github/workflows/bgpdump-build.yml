name: C/C++ CI for bgpdump

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: bootstrap
      run: ./bootstrap.sh
    - name: make
      run: make
    - name: run tests
      run: ./test.sh
      
